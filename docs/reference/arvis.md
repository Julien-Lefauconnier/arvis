# `arvis`: the root surface

The recommended facade is `ArvisEngine`; `CognitiveOS` is the
runtime it delegates to. One instance governs one turn.

::: arvis
    options:
      show_submodules: false
      filters:
        - "!^_"
