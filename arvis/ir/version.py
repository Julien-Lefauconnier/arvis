# arvis/ir/version.py

# The cognitive-IR SCHEMA version and fingerprint (campaign HARDEN,
# DM-H9e: three constants named IR_VERSION carried three different
# meanings; each now says which one it is). Wire values unchanged.
IR_SCHEMA_VERSION = "1.0"
IR_SCHEMA_FINGERPRINT = "arvis-cognitive-ir-v1"

# Compatibility aliases (internal surface).
IR_VERSION = IR_SCHEMA_VERSION
IR_FINGERPRINT = IR_SCHEMA_FINGERPRINT
